#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print the numbers from 0 to 10
 * Return: 0 Always (success)
 */
int main(void)
{

	int i;

	for (i = '0'; i <= '9'; i++)
	{
		putchar(i);
	}
	{
		putchar('\n');
	}
	return (0);
}
