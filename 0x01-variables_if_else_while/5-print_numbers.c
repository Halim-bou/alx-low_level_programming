#include <stdio.h>
/**
 * main - print the numbers from 0 to 10
 * Return: 0 Always (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	{
		printf("\n");
	}
	return (0);
}
